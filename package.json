{
  "name": "mernify",
  "version": "1.0.0",
  "description": "An ecommerce project based on MERN stack",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\" ",
    "update-all": "concurrently \"cd backend && npm i \" \"cd frontend && npm i \""
  },
  "author": "Akshat Anand",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
